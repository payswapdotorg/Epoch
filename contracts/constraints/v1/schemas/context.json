{
  "$defs": {
    "__schema0": {
      "anyOf": [
        {
          "type": "null"
        },
        {
          "type": "boolean"
        },
        {
          "type": "number"
        },
        {
          "type": "string"
        },
        {
          "items": {
            "$ref": "#/$defs/__schema0"
          },
          "type": "array"
        },
        {
          "additionalProperties": {
            "$ref": "#/$defs/__schema0"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        }
      ]
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Typed evaluation context consumed by the deterministic evaluator.",
  "properties": {
    "inputs": {
      "additionalProperties": {
        "$ref": "#/$defs/__schema0"
      },
      "description": "Input slot values keyed by declared input name. Validated per-constraint against the compiled input declarations (exact keys, exact types; numbers must be finite).",
      "propertyNames": {
        "type": "string"
      },
      "type": "object"
    }
  },
  "required": [
    "inputs"
  ],
  "type": "object"
}
