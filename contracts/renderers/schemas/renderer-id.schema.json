{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/RendererId",
  "$defs": {
    "RendererId": {
      "type": "string",
      "pattern": "^rr-[a-z0-9][a-z0-9-]{0,62}$",
      "title": "RendererId",
      "description": "Renderer-descriptor identifier: \"rr-\" followed by a lowercase slug (never a vendor product)."
    }
  }
}
