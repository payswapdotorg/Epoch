# @epoch/web

Minimal Next.js 15 (App Router) + React 19 app shell placeholder owned by W001.
It renders "Epoch" and nothing else — no UI features, no styling systems, no
state libraries. The real web shell is delivered by W014 (see
spec/work-items.md); W014 owns `apps/web/app/*`, `apps/web/src/shell/*` and
`apps/web/src/shared/*`.
