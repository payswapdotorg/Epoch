# packs/

Reserved for Epoch domain packs (W026+): `construction` (W026) and
`software` (W027).

W001 intentionally creates no pack packages — each pack is introduced by its
owning Work Order (see spec/work-items.md). Packs import only contracts /
kernel capability APIs (see scripts/boundary-check.mjs for the layer model).
