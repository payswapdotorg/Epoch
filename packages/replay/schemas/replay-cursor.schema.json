{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/ReplayCursor",
  "$defs": {
    "ReplayCursor": {
      "readOnly": true,
      "title": "ReplayCursor",
      "description": "Per-stream resume coordinate: events with sequence > lastSequence apply next.",
      "type": "object",
      "properties": {
        "streamId": {
          "$ref": "#/$defs/EventStreamId"
        },
        "lastSequence": {
          "$ref": "#/$defs/EventSequence"
        }
      },
      "required": [
        "streamId",
        "lastSequence"
      ],
      "additionalProperties": false
    },
    "EventStreamId": {
      "type": "string",
      "pattern": "^stream:[a-z0-9][a-z0-9-]{0,62}$",
      "title": "EventStreamId",
      "description": "Opaque event stream identity: the unit of total order; every event in a stream carries a contiguous 1-based sequence."
    },
    "EventSequence": {
      "type": "integer",
      "minimum": 1,
      "maximum": 9007199254740991,
      "title": "EventSequence",
      "description": "One event sequence number: 1-based, strictly contiguous per stream (the stream cursor)."
    }
  },
  "$id": "urn:epoch:replay:replay-cursor:1.0.0"
}
