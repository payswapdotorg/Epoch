{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/WorldAdvanceFrameEnvelope",
  "$defs": {
    "WorldAdvanceFrameEnvelope": {
      "type": "object",
      "properties": {
        "schema": {
          "type": "string",
          "const": "epoch.renderer-invocation"
        },
        "protocolVersion": {
          "type": "string",
          "const": "1.0.0"
        },
        "kind": {
          "type": "string",
          "const": "advance-frame"
        },
        "invocationId": {
          "$ref": "#/$defs/WorldInvocationId"
        },
        "rendererSessionId": {
          "$ref": "#/$defs/WorldRendererSessionId"
        },
        "frameIndex": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991
        },
        "atMs": {
          "$ref": "#/$defs/WorldVirtualTimeMs"
        }
      },
      "required": [
        "schema",
        "protocolVersion",
        "kind",
        "invocationId",
        "rendererSessionId",
        "frameIndex",
        "atMs"
      ],
      "additionalProperties": false,
      "title": "WorldAdvanceFrameEnvelope",
      "description": "Advance-frame invocation envelope emitted by the world-experience compiler (mirrored W013 shape; monotonic frame index)."
    },
    "WorldInvocationId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$",
      "title": "WorldInvocationId",
      "description": "Opaque invocation identifier (shared message-id grammar), unique within the caller scope."
    },
    "WorldRendererSessionId": {
      "type": "string",
      "pattern": "^rs-[a-z0-9][a-z0-9-]{0,62}$",
      "title": "WorldRendererSessionId",
      "description": "Renderer-session identifier (mirrored shared grammar; canonical home @epoch/renderer-runtime): \"rs-\" + lowercase slug."
    },
    "WorldVirtualTimeMs": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "title": "WorldVirtualTimeMs",
      "description": "Virtual time in non-negative integer milliseconds (caller-supplied; never a wall clock)."
    }
  },
  "$id": "urn:epoch:world-experience:world-advance-frame-envelope:1.0.0"
}
