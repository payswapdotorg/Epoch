{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/ResourceReference",
  "$defs": {
    "ResourceReference": {
      "readOnly": true,
      "title": "ResourceReference",
      "description": "The tenancy-scoped resource a request acts on: opaque type and id plus the optional tenant/workspace/project scope (a resource without a tenant is platform-scoped).",
      "type": "object",
      "properties": {
        "resourceType": {
          "type": "string",
          "minLength": 1,
          "maxLength": 256
        },
        "resourceId": {
          "type": "string",
          "minLength": 1,
          "maxLength": 256
        },
        "tenantId": {
          "$ref": "#/$defs/TenantId"
        },
        "workspaceId": {
          "$ref": "#/$defs/WorkspaceId"
        },
        "projectId": {
          "$ref": "#/$defs/ProjectId"
        }
      },
      "required": [
        "resourceType",
        "resourceId"
      ],
      "additionalProperties": false
    },
    "TenantId": {
      "type": "string",
      "pattern": "^tenant:[a-z0-9][a-z0-9-]{0,62}$",
      "title": "TenantId",
      "description": "Opaque tenant id: \"tenant:\" followed by a lowercase slug (mirrors @epoch/tenancy)."
    },
    "WorkspaceId": {
      "type": "string",
      "pattern": "^workspace:[a-z0-9][a-z0-9-]{0,62}$",
      "title": "WorkspaceId",
      "description": "Opaque workspace id: \"workspace:\" followed by a lowercase slug (mirrors @epoch/tenancy)."
    },
    "ProjectId": {
      "type": "string",
      "pattern": "^project:[a-z0-9][a-z0-9-]{0,62}$",
      "title": "ProjectId",
      "description": "Opaque project id: \"project:\" followed by a lowercase slug (mirrors @epoch/tenancy)."
    }
  },
  "$id": "urn:epoch:authorization:resource-reference:1.0.0"
}
