{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/PrincipalFact",
  "$defs": {
    "PrincipalFact": {
      "readOnly": true,
      "title": "PrincipalFact",
      "description": "Caller-supplied principal fact: status and authentication state projected from @epoch/identity records by the host.",
      "type": "object",
      "properties": {
        "principalId": {
          "$ref": "#/$defs/PrincipalId"
        },
        "status": {
          "$ref": "#/$defs/PrincipalStatus"
        },
        "authenticated": {
          "type": "boolean"
        }
      },
      "required": [
        "principalId",
        "status",
        "authenticated"
      ],
      "additionalProperties": false
    },
    "PrincipalId": {
      "type": "string",
      "pattern": "^principal:[a-z0-9][a-z0-9-]{0,62}$",
      "title": "PrincipalId",
      "description": "Opaque principal id: \"principal:\" followed by a lowercase slug (mirrors @epoch/identity)."
    },
    "PrincipalStatus": {
      "type": "string",
      "enum": [
        "active",
        "suspended",
        "deactivated"
      ],
      "title": "PrincipalStatus",
      "description": "Principal status fact (mirrors @epoch/identity): active, suspended, or deactivated."
    }
  },
  "$id": "urn:epoch:authorization:principal-fact:1.0.0"
}
