{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/TenancySnapshot",
  "$defs": {
    "TenancySnapshot": {
      "readOnly": true,
      "title": "TenancySnapshot",
      "description": "Deterministic serialization of a whole tenancy directory: all nodes, sorted by id (insertion order never leaks).",
      "type": "object",
      "properties": {
        "schemaVersion": {
          "$ref": "#/$defs/TenancyRecordVersion"
        },
        "nodes": {
          "readOnly": true,
          "type": "array",
          "items": {
            "$ref": "#/$defs/TenancyNode"
          }
        }
      },
      "required": [
        "schemaVersion",
        "nodes"
      ],
      "additionalProperties": false
    },
    "TenancyRecordVersion": {
      "type": "number",
      "const": 1,
      "title": "TenancyRecordVersion",
      "description": "Version discriminator carried by every serialized tenancy document (currently 1)."
    },
    "TenancyNode": {
      "readOnly": true,
      "title": "TenancyNode",
      "description": "A tenancy hierarchy node: typed id (prefix = kind), kind, an opaque parent-id reference (null only for the platform root), and an optional display name.",
      "type": "object",
      "properties": {
        "schemaVersion": {
          "$ref": "#/$defs/TenancyRecordVersion"
        },
        "id": {
          "$ref": "#/$defs/TenancyNodeId"
        },
        "kind": {
          "$ref": "#/$defs/TenancyNodeKind"
        },
        "parentId": {
          "anyOf": [
            {
              "$ref": "#/$defs/TenancyNodeId"
            },
            {
              "type": "null"
            }
          ]
        },
        "displayName": {
          "type": "string",
          "minLength": 1,
          "maxLength": 256
        }
      },
      "required": [
        "schemaVersion",
        "id",
        "kind",
        "parentId"
      ],
      "additionalProperties": false
    },
    "TenancyNodeId": {
      "type": "string",
      "pattern": "^(platform|tenant|workspace|project|world|scenario|evidence):[a-z0-9][a-z0-9-]{0,62}$",
      "title": "TenancyNodeId",
      "description": "Typed tenancy-node identity: a known kind prefix (\"tenant:\", \"workspace:\", ...) followed by a lowercase slug; the prefix encodes the node kind."
    },
    "TenancyNodeKind": {
      "type": "string",
      "enum": [
        "platform",
        "tenant",
        "workspace",
        "project",
        "world",
        "scenario",
        "evidence"
      ],
      "title": "TenancyNodeKind",
      "description": "Tenancy hierarchy level: platform, tenant, workspace, project, or the project-scoped leaves world/scenario/evidence."
    }
  },
  "$id": "urn:epoch:tenancy:tenancy-snapshot:1.0.0"
}
