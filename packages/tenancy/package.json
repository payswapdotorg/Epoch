{
  "name": "@epoch/tenancy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Epoch Tenancy kernel — the typed, provider-neutral tenancy hierarchy Platform -> Tenant -> Workspace -> Project -> World/Scenario/Evidence as opaque-id references, with deterministic in-memory create/resolve/validate machinery, cycle and hierarchy-escape rejection, tenant isolation checks, and content-addressed node records (W009).",
  "epoch": {
    "layer": "kernel"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@epoch/agent-protocol": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@epoch/eslint-config": "workspace:*",
    "@epoch/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
