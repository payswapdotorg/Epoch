{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/ObservationReference",
  "$defs": {
    "ObservationReference": {
      "readOnly": true,
      "title": "ObservationReference",
      "description": "One W036 Observation-distinction record reference: the kind-prefixed record id plus its exact content digest (the DeliveryRecord observation intake linkage).",
      "type": "object",
      "properties": {
        "recordId": {
          "type": "string",
          "pattern": "^observation:[a-z0-9][a-z0-9-]{0,62}$"
        },
        "contentDigest": {
          "$ref": "#/$defs/Sha256Hex"
        }
      },
      "required": [
        "recordId",
        "contentDigest"
      ],
      "additionalProperties": false
    },
    "Sha256Hex": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "title": "Sha256Hex",
      "description": "Lowercase hexadecimal SHA-256 digest (exactly 64 characters)."
    }
  },
  "$id": "urn:epoch:procurement:observation-reference:1.0.0"
}
