{
  "name": "@epoch/policy-contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Epoch policy-side typed contracts kernel: policy documents, applicability scope, precedence and composition over constraint evaluations (W004).",
  "epoch": {
    "layer": "kernel"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint . && eslint ../../contracts/constraints/v1",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@epoch/constraint-language": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@epoch/eslint-config": "workspace:*",
    "@epoch/tsconfig": "workspace:*",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
