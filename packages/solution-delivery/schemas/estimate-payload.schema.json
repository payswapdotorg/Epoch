{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/EstimatePayload",
  "$defs": {
    "EstimatePayload": {
      "readOnly": true,
      "title": "EstimatePayload",
      "description": "Estimate payload: optional opaque estimation-method reference and optional low/high range in the measure unit.",
      "type": "object",
      "properties": {
        "method": {
          "$ref": "#/$defs/OpaqueReference"
        },
        "range": {
          "readOnly": true,
          "type": "object",
          "properties": {
            "low": {
              "$ref": "#/$defs/NonNegativeDecimal"
            },
            "high": {
              "$ref": "#/$defs/NonNegativeDecimal"
            }
          },
          "required": [
            "low",
            "high"
          ],
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "OpaqueReference": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "title": "OpaqueReference",
      "description": "Opaque, bounded, provider-neutral reference string owned by its producing system."
    },
    "NonNegativeDecimal": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)(\\.[0-9]+)?$",
      "title": "NonNegativeDecimal",
      "description": "Non-negative decimal amount as a canonical string (digest-safe, no exponent form)."
    }
  },
  "$id": "urn:epoch:solution-delivery:estimate-payload:1.0.0"
}
