{
  "name": "@epoch/identity",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Epoch Identity kernel — provider-neutral principal modeling: typed principals and principal ids, credential-assertion descriptors, and authentication-result records with typed failure reasons; zero concrete IdPs, zero network, zero secrets storage (W009).",
  "epoch": {
    "layer": "kernel"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@epoch/agent-protocol": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@epoch/eslint-config": "workspace:*",
    "@epoch/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
