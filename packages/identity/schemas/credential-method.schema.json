{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/CredentialMethod",
  "$defs": {
    "CredentialMethod": {
      "type": "string",
      "enum": [
        "knowledge",
        "possession",
        "inherence",
        "signature",
        "attestation"
      ],
      "title": "CredentialMethod",
      "description": "Credential method class: knowledge, possession, inherence, signature, or attestation — names the factor kind, never the vendor that issued or verified it."
    }
  },
  "$id": "urn:epoch:identity:credential-method:1.0.0"
}
