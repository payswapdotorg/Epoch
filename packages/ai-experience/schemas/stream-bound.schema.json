{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/StreamBound",
  "$defs": {
    "StreamBound": {
      "readOnly": true,
      "title": "StreamBound",
      "description": "One known stream bound: the stream id and its last applied event sequence.",
      "type": "object",
      "properties": {
        "streamId": {
          "$ref": "#/$defs/EventStreamId"
        },
        "lastSequence": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991
        }
      },
      "required": [
        "streamId",
        "lastSequence"
      ],
      "additionalProperties": false
    },
    "EventStreamId": {
      "type": "string",
      "pattern": "^stream:[a-z0-9][a-z0-9-]{0,62}$",
      "title": "EventStreamId",
      "description": "Opaque event stream identity: the unit of total order; every event in a stream carries a contiguous 1-based sequence."
    }
  },
  "$id": "urn:epoch:ai-experience:stream-bound:1.0.0"
}
